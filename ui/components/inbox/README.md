# Product Inbox

InboxPane is a Quadrant-Tasks component. It renders the Product-owned InboxItem
DTO from ui/product and emits callbacks for adapters to handle. Domain state,
persistence and IPC stay outside Slint. Generic controls are imported through
@quadrant-kit. This task-specific component is not part of the Kit public API.
